{
	"name": "mealplan-github-kv",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "bash -c 'HASH=$(git rev-parse --short HEAD 2>/dev/null || echo dev) && cp frontend/version.js frontend/version.js.bak && sed -i \"s/__GIT_HASH__/$HASH/g\" frontend/version.js && wrangler deploy; EXIT=$?; mv frontend/version.js.bak frontend/version.js; exit $EXIT'",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.14.1",
		"@cloudflare/workers-types": "^4.20250327.0",
		"typescript": "^5.5.2",
		"vitest": "^4.1.2",
		"wrangler": "^4.6.0"
	}
}
